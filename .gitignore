# Environment
.env
*.local

# SSH deploy key — generated per host, never committed
manager/ssh/*
!manager/ssh/SETUP.md

# Chrony custom configuration
chrony/chrony.conf.d/*
!chrony/chrony.conf.d/README.md

# Unbound custom configuration
unbound/custom.conf.d/*
!unbound/custom.conf.d/README.md

