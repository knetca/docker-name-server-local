# 20-blocklist.conf — placeholder blocklist
# Baked into dns-manager image. Copied to zones volume on first start only,
# if no blocklist is present. Replaced by update-blocklist.sh on first
# successful OISD fetch.
#
# Empty file is syntactically valid for Unbound — no ad blocking until
# the first fetch completes.
